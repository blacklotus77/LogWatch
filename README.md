# LogWatch
Flask web app that analyzes Apache/Nginx access logs to flag suspicious activity. Detects wp-login scans, .env probes, path traversal, and SQLi attempts. Shows top IPs, status codes, hourly traffic sparkline, and exports flagged IPs to TXT. Built with Python + Regex.
